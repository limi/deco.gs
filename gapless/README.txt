This is an experimental variant with no margins, contributed by Denis Mishunov.

It is useful when you have layouts where the elements are positioned right
next to each other with no whitespace.

Gapless grids might have about 1px gaps sometimes due to rounding of percentage
values for the widths.
